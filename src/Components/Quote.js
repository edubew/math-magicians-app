import React from 'react';

const Quote = () => (
  <div>
    <p>
      `It is not knowledge, but the act of learning, not possession but the act
      of getting there, which grants the greatest enjoyment.` ~ Carl Friedrich
      Gauss
    </p>
  </div>
);

export default Quote;
